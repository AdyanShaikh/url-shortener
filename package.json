{
  "name": "url-shortener",
  "version": "1.0.0",
  "description": "Minimal HTTP URL shortener: create, follow, and count links",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test test/*.test.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2"
  }
}
